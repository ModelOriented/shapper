language: r
install:
  - sudo apt-get install -y python3 python-numpy python-scipy 
sudo: true
cache: packages

install:
 - pip install --upgrade pip
 - pip install shap
      
r_packages:
  - covr

after_success:
- Rscript -e 'library(covr); codecov()'
