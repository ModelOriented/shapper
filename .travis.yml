language: r
sudo: false
cache: packages

addons:
  apt:
    packages:
      - python3.6
      - python-numpy
      - python-scipy
      - python-shap
      
 r_packages:
  - covr

after_success:
- Rscript -e 'library(covr); codecov()'
