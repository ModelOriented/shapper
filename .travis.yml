language: r
install:
  - sudo apt-get install -y python3 python-numpy python-scipy python3-pip
sudo: true
cache: packages

install:
 - pip3 install --upgrade pip
 - pip3 install shap
      
r_packages:
  - covr

after_success:
- Rscript -e 'library(covr); codecov()'
