language: r
install:
  - sudo apt-get install -y python3.6 python-numpy python-scipy python-shap
sudo: true
cache: packages
      
r_packages:
  - covr

after_success:
- Rscript -e 'library(covr); codecov()'
